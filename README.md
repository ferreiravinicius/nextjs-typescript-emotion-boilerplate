🔫 Boilerplate for NextJS + Typescript + Emotion

**Troubleshooting**

Delete `.next` and `node_modules` and restart project in case of errors.

`sh
  > rm -rf .next
  > rm -rf node_modules
  > npm i
  > npm run dev
`